This folder contains the HDL code for a bit-accessor which tells whether that particular bit is set high or not, which will be
then sent to the 8 to 3 one hot encoder which will give the register's address in the RF
This is a non-clocked (asynchronous) component.
It will be used only in the instructions SM and LM.
